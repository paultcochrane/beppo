#!/usr/bin/env perl6

use v6;

sub MAIN(Str :$config, Str :$start-date) {
    if $config {
        read_config($config);
    }
}

sub read_config($config) {
    if not $config.IO.e {
        die "Config file '$config' does not exist";
    }
}

# vim: expandtab shiftwidth=4 softtabstop=4 ft=perl6
